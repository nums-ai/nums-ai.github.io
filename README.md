# nums.world

Marketing homepage for Nums AI. Built with Next.js (static export) and deployed to GitHub Pages.

## Development

```bash
npm install
npm run dev     # local dev server
npm run build   # static export to out/
```

Pushing to `main` triggers a GitHub Actions workflow that publishes `out/` to GitHub Pages.
